import { defineConfig } from 'astro/config';

export default defineConfig({
  // Sitio de usuario: URL raiz. El repo debe llamarse egutierrezp23.github.io
  site: 'https://egutierrezp23.github.io',
  output: 'static',
});
