import { defineConfig } from 'astro/config';

export default defineConfig({
  // Sitio de usuario: URL raiz. El repo debe llamarse egutierrezcr.github.io
  site: 'https://egutierrezcr.github.io',
  output: 'static',
});
